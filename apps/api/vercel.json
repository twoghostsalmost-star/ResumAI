{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile=false",
  "buildCommand": "cd ../.. && pnpm build:shared && pnpm --filter @resumeforge/api exec prisma generate && pnpm --filter @resumeforge/api build && pnpm --filter @resumeforge/api exec prisma db push --skip-generate --accept-data-loss",
  "outputDirectory": "dist",
  "functions": {
    "api/index.mjs": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index.mjs" }]
}
